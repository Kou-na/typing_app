.Main
  .User-part
  
    .Page-title
      = link_to(content_tag(:i, ' 戻る', class: 'far fa-arrow-alt-circle-left'), root_path, class: 'Page-title__btn')
      .Page-title__contents
        %h1
          文字の一覧
      .help-icon
        = icon('far', 'question-circle', class: 'Page-title__btn--question')
        .help
          %p 削除と編集の画面へは
          %br 文字のクリックで
          %br 行くことができます。
          %br 

    .Table
      %table
        %thead
          %tr.table-head
            %th
              質問
            %th
              答え
            %th
              打つ文字
      %table
        %tbody.table-body
          - @words.each do |word|
            %tr
              %td
                = link_to word.question, edit_word_path(word.id)
              %td
                = link_to word.answer, edit_word_path(word.id)
              %td
                = link_to word.target, edit_word_path(word.id)